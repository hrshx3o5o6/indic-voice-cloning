---
phase: 03-cli-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/indic_voice/cli.py
autonomous: true
requirements:
  - CLI-01
  - CLI-02
user_setup: []

must_haves:
  truths:
    - "clone command auto-transcribes ref_voice via Whisper if --ref-text not provided"
    - "translate command passes Whisper transcript as ref_text to IndicF5"
    - "Both commands work without requiring explicit ref_text"
  artifacts:
    - path: "src/indic_voice/cli.py"
      provides: "Auto-transcription logic in clone and translate"
      min_lines: 80
  key_links:
    - from: "clone command"
      to: "transcribe_audio()"
      via: "auto-transcribe when ref_text is None"
      pattern: "transcribe_audio.*ref_voice"
    - from: "translate command"
      to: "indicf5_generate_speech()"
      via: "ref_text parameter"
      pattern: "ref_text.*en_text"
---

<objective>
Implement auto-transcription for ref_text: clone command auto-transcribes ref_voice if --ref-text not provided, and translate command threads Whisper transcript through to IndicF5.

Purpose: Zero breaking changes - users no longer need to supply ref_text manually.
Output: Both commands work without explicit ref_text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/indic_voice/cli.py (After Plan 01 changes)
@src/indic_voice/pipeline/asr.py (transcribe_audio function)
@.planning/REQUIREMENTS.md (CLI-01, CLI-02 requirements)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auto-transcription logic to clone command</name>
  <files>src/indic_voice/cli.py</files>
  <read_first>
    - src/indic_voice/cli.py (clone function after Plan 01)
    - src/indic_voice/pipeline/asr.py (transcribe_audio signature)
  </read_first>
  <action>
    Add auto-transcription logic before calling IndicF5:

    1. After the "1. Generating cloned speech..." print, add:

    ```python
    # Auto-transcribe ref_voice if ref_text not provided (CLI-01)
    if ref_text is None:
        console.print("   [dim]Auto-transcribing reference audio...[/dim]")
        ref_text = transcribe_audio(ref_voice)
        console.print(f"   [dim]Ref transcript: '{ref_text}'[/dim]")
    ```

    2. Ensure the ref_text parameter is passed to indicf5_generate_speech()

    The transcribe_audio function from asr.py takes an audio_path and returns text.
  </action>
  <verify>
    <automated>grep -n "transcribe_audio(ref_voice)" src/indic_voice/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - clone command calls transcribe_audio(ref_voice) when ref_text is None
    - The ref_text is then passed to indicf5_generate_speech
    - A progress message is printed during auto-transcription
  </acceptance_criteria>
  <done>Clone command auto-transcribes when ref_text not provided</done>
</task>

<task type="auto">
  <name>Task 2: Wire translate command to use IndicF5 with ref_text</name>
  <files>src/indic_voice/cli.py</files>
  <read_first>
    - src/indic_voice/cli.py (translate function after Plan 01 - should still reference Sarvam)
  </read_first>
  <action>
    Modify the translate_audio() command:

    1. Replace the imports if needed - should already have transcribe_audio imported

    2. Replace the translate command body to:
       - Step 1: Transcribe English audio with Whisper (already exists)
       - Step 2: Translate to target language (already exists)
       - Step 3: Generate with IndicF5 (REPLACE Sarvam call)
       - Step 4: REMOVE the OpenVoice tone morph step (IndicF5 does voice cloning)

    3. Replace Step 3 and 4 with IndicF5 call:
    ```python
    console.print("3. Generating cloned speech via IndicF5...")
    indicf5_generate_speech(
        text=hi_text,
        ref_audio_path=audio,  # Use source audio as reference
        ref_text=en_text,       # Use Whisper transcript as ref_text (CLI-02)
        output_path=output,
    )
    ```

    4. Remove the tmp_tts file handling (no longer needed)

    The key difference: IndicF5 does voice cloning in one pass, so we pass the source audio (audio parameter) as the reference voice, and the English transcript as ref_text.
  </action>
  <verify>
    <automated>grep -n "indicf5_generate_speech" src/indic_voice/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - translate command uses indicf5_generate_speech
    - ref_text is set to en_text (Whisper transcript) - CLI-02
    - ref_audio_path is set to audio (source audio) - voice cloning from source
    - No Sarvam or OpenVoice calls remain in translate command
  </acceptance_criteria>
  <done>Translate command uses IndicF5 with Whisper transcript as ref_text</done>
</task>

<task type="auto">
  <name>Task 3: Clean up unused imports after migration</name>
  <files>src/indic_voice/cli.py</files>
  <read_first>
    - src/indic_voice/cli.py (imports section)
  </read_first>
  <action>
    After the previous tasks, verify and clean up imports:

    1. Verify the following imports exist:
       - `from indic_voice.pipeline.asr import transcribe_audio`
       - `from indic_voice.pipeline.translator import translate`
       - `from indic_voice.pipeline.tts_indicf5 import generate_speech as indicf5_generate_speech`

    2. Remove any unused imports:
       - If `tts_sarvam` is still imported, remove it
       - If `tone_transfer` is still imported, remove it

    3. Keep the `import os` for potential temp file handling (or remove if not needed)
  </action>
  <verify>
    <automated>grep -E "^from|^import" src/indic_voice/cli.py | grep -v "typer\|rich\|os"</automated>
  </verify>
  <acceptance_criteria>
    - No tts_sarvam import exists
    - No tone_transfer import exists
    - Only pipeline imports are: asr, translator, tts_indicf5
  </acceptance_criteria>
  <done>Imports cleaned up - only needed imports remain</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → CLI | Untrusted text and audio paths cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | transcribe_audio | accept | Whisper runs locally; no data leaves machine |
| T-03-04 | Tampering | audio parameter reuse | accept | Source audio used as ref - expected S2ST behavior |
</threat_model>

<verification>
- `uv run indic-voice clone --text "नमस्ते" --ref-voice ref.wav` runs without --ref-text (auto-transcribes)
- `uv run indic-voice translate --audio input.wav --target-lang hi` runs end-to-end
</verification>

<success_criteria>
1. clone command auto-transcribes ref_voice when --ref-text not provided
2. translate command uses Whisper transcript as ref_text
3. No manual ref_text required for either command
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-integration/03-02-SUMMARY.md`
</output>