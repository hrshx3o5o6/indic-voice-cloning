---
phase: 03-cli-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/indic_voice/cli.py
autonomous: true
requirements:
  - CLI-07
  - CLI-03
user_setup: []

must_haves:
  truths:
    - "clone command uses IndicF5 instead of Sarvam+OpenVoice pipeline"
    - "Optional --ref-text flag added to clone command"
    - "All existing flags work identically to before"
  artifacts:
    - path: "src/indic_voice/cli.py"
      provides: "CLI command definitions"
      min_lines: 50
    - path: "src/indic_voice/pipeline/tts_indicf5.py"
      provides: "IndicF5 TTS generation"
      exports: ["generate_speech"]
  key_links:
    - from: "cli.py clone()"
      to: "tts_indicf5.generate_speech"
      via: "import and call"
      pattern: "from indic_voice.pipeline.tts_indicf5 import"
---

<objective>
Replace the Sarvam+OpenVoice pipeline in the `clone` command with IndicF5, adding the optional --ref-text flag.

Purpose: Wire the IndicF5 module (built in Phase 2) into the CLI without breaking existing users.
Output: Modified clone command that uses IndicF5 for TTS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/indic_voice/cli.py (Current CLI - uses Sarvam + OpenVoice)
@src/indic_voice/pipeline/tts_indicf5.py (IndicF5 module from Phase 2)
@.planning/REQUIREMENTS.md (CLI-01 through CLI-07 requirements)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace imports (Sarvam+OpenVoice → IndicF5)</name>
  <files>src/indic_voice/cli.py</files>
  <read_first>
    - src/indic_voice/cli.py (current imports and structure)
  </read_first>
  <action>
    1. Remove the imports for Sarvam TTS and tone transfer:
       - Remove: `from indic_voice.pipeline.tts_sarvam import generate_speech`
       - Remove: `from indic_voice.models.tone_transfer import morph_tone`

    2. Add the IndicF5 import:
       - Add: `from indic_voice.pipeline.tts_indicf5 import generate_speech as indicf5_generate_speech`

    Keep the existing imports: `transcribe_audio`, `translate`
  </action>
  <verify>
    <automated>grep -n "from indic_voice.pipeline.tts_indicf5 import" src/indic_voice/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - cli.py contains `from indic_voice.pipeline.tts_indicf5 import generate_speech as indicf5_generate_speech`
    - cli.py does NOT contain `tts_sarvam` import
    - cli.py does NOT contain `tone_transfer` import
  </acceptance_criteria>
  <done>Imports updated to use IndicF5</done>
</task>

<task type="auto">
  <name>Task 2: Add optional --ref-text flag to clone command</name>
  <files>src/indic_voice/cli.py</files>
  <read_first>
    - src/indic_voice/cli.py (clone command definition, lines 18-42)
  </read_first>
  <action>
    Add new optional parameter to the clone() function:

    ```python
    ref_text: str = typer.Option(None, "--ref-text", help="Transcript of the reference audio (auto-generated if not provided)"),
    ```

    This must come AFTER the existing `--ref-voice` option and BEFORE `--output`.
  </action>
  <verify>
    <automated>grep -n "ref_text.*typer.Option" src/indic_voice/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - clone command accepts `--ref-text` as optional flag
    - The flag has help text mentioning auto-generation
    - Existing flags (--text, --ref-voice, --output) remain unchanged
  </acceptance_criteria>
  <done>Optional ref_text flag added to clone command</done>
</task>

<task type="auto">
  <name>Task 3: Wire IndicF5 into clone command body</name>
  <files>src/indic_voice/cli.py</files>
  <read_first>
    - src/indic_voice/cli.py (clone function body, lines 27-41)
  </read_first>
  <action>
    Replace the clone command body to use IndicF5:

    1. Remove the entire old pipeline (Sarvam + OpenVoice):
       - Remove: `tmp_tts = "tmp_sarvam_tts.wav"` 
       - Remove: `generate_speech(text, tmp_tts)` call
       - Remove: `morph_tone(tmp_tts, ref_voice, output)` call
       - Remove: the finally block that cleans up tmp_tts

    2. Add new IndicF5 call:
       ```python
       console.print("2. Generating cloned speech via IndicF5...")
       indicf5_generate_speech(
           text=text,
           ref_audio_path=ref_voice,
           ref_text=ref_text,  # Will be None if not provided - need auto-transcribe
           output_path=output,
       )
       ```

    Keep the existing Step 1 progress print and the success/error output.
  </action>
  <verify>
    <automated>grep -n "indicf5_generate_speech" src/indic_voice/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - clone command calls indicf5_generate_speech with text, ref_audio_path, ref_text, output_path
    - No Sarvam or OpenVoice calls remain in clone command
    - Output path parameter name is output_path (matching IndicF5 signature)
  </acceptance_criteria>
  <done>Clone command uses IndicF5 for TTS generation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → CLI | Untrusted text and audio paths cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | clone command | mitigate | CLI-04 (audio duration validation) added in later plan |
| T-03-02 | Information Disclosure | ref_text parameter | accept | User supplies transcript; no confidentiality risk |
</threat_model>

<verification>
- `uv run indic-voice clone --help` shows --ref-text as optional
- `uv run indic-voice clone --text "नमस्ते" --ref-voice ref.wav` runs without error (will fail on ref_text=None - addressed in next plan)
</verification>

<success_criteria>
1. clone command uses IndicF5 instead of Sarvam+OpenVoice
2. Optional --ref-text flag is available
3. All existing flags (--text, --ref-voice, --output) work identically
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-integration/03-01-SUMMARY.md`
</output>